<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartVenueAI">

        <!-- Launcher: Login Screen -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Screen (Map + Bottom Nav + Drawer) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Navigate Detail (3D Route) -->
        <activity
            android:name=".ui.navigation.NavigateDetailActivity"
            android:exported="false" />

        <!-- Alerts & Live Feed -->
        <activity
            android:name=".ui.alerts.AlertsLiveActivity"
            android:exported="false" />

        <!-- Complete Your Profile -->
        <activity
            android:name=".ui.profile.CompleteProfileActivity"
            android:exported="false" />

    </application>

</manifest>